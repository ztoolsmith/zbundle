// A minimal automatic JSX runtime, under a name that is NOT react — which is
// the whole point: the bundle must import from here because the tsconfig said
// `jsxImportSource: "preact"`, not because of a default.
export const jsx = (type, props) => ({ runtime: 'preact', type, props });
export const jsxs = jsx;
export const Fragment = 'fragment';
